%------------------MODELL-----------------------------------%

% umstiegszeit(HALT, UMSTEIGEZEIT).

umstiegszeit(halt1, 5).
umstiegszeit(halt2, 2).
umstiegszeit(halt3, 3).
umstiegszeit(halt4, 5).
umstiegszeit(halt5, 3).
